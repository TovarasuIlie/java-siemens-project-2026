spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/siemens_trains_2026?forceBinary=false&reWriteBatchedInserts=true
    username: siemens_app
    password: ParolaTare
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Europe/Bucharest

  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: adresa_ta_de_email@gmail.com
    password: parola_ta_de_aplicatie
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true